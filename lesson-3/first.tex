\documentclass[a4paper, 12pt]{article}
\usepackage[T1]{fontenc}
% square brackets are optional args
% curly braces are mandatory arguments

% this is a comment
% document pre-ample is the code above \begin{document}, which sets up the document layout
% \begin{...} and \end{...} pairs are called environments; must match
\begin{document}

% \begin{nested_doc}
% Hello, world!
% \end{nested_doc}


This is a       simple document\footnote{with a footnote}.

% multiple spaces are treated as a single space.

This is a 2 new paragraph.

Use tilde (\textasciitilde) for hard space ~~~~ like this; useful for creating cross-references.


\end{document}

% compile this document using the "compile" or "typeset" button on the GUI
% or type `pdflatex <filename>` in the terminal