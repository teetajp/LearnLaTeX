\documentclass{article}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\begin{document}

Solve the following recurrence for 
$ n,k\geq 0 $:
\begin{align*}
	Q_{n,0} &= 1	\quad Q_{0,k} = [k=0]; \\
	Q_{n,k} &= Q_{n-1,k}+Q_{n-1,k-1}+\binom{n}{k}, \quad\text{for $n$, $k>0$.}
\end{align*}
% the `align*` environment makes equations line up on the ampersand (the & symbols) just like a table
% \quad inserts space
% \text puts normal text inside math mode

% most math environments number the equations by default, and the starred variant (*) disables numbering.

AMS matrices.
\[
\begin{matrix}
a & b & c \\
d & e & f
\end{matrix}
\quad
\begin{pmatrix}
a & b & c \\
d & e & f
\end{pmatrix} % 'p' for parentheses
\quad
\begin{bmatrix} % 'b' for brackets
a & b & c \\
d & e & f
\end{bmatrix}
\]


% Fonts
% \mathrm: roman
% \mathit: italic spaced as 'text'
% \mathbf: boldface
% \mathsf: sans serif
% \mathtt: monospaced (typewriter)
% \mathbb: double-struck (blackboard bold) (from `amsfonts` package).
Fonts\\
The matrix $\mathrm{M}$. \\
The matrix $\mathit{M}$. \\
The matrix $\mathbf{M}$. \\
The matrix $\mathsf{M}$. \\
The matrix $\mathtt{M}$. \\
The matrix $\mathbb{M}$. \\

$\text{bad use } size  \neq \mathit{size} \neq \mathrm{size} $

\textit{$\text{bad use } size \neq \mathit{size} \neq \mathrm{size} $}

\end{document}